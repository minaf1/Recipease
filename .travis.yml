language: java
sudo: true
dist: trusty
jdk: oraclejdk9
addons:
  apt:
    packages:
      - oracle-java9-installer
before_install:
  - chmod +x mvnw
after_success:
  - docker build -t gcr.io/	project2-251819/recipease:v1 .
